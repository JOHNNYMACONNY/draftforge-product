# DraftForge Product — Archived staging repo

This repository was a temporary cleanup/staging repo.

The canonical public DraftForge repo is:

https://github.com/JOHNNYMACONNY/draftforge

Install from the canonical repo:

```bash
npm install -g github:JOHNNYMACONNY/draftforge
draftforge --help
```

Do not use this repo for new installs, agent skills, IDE plugins, or docs.
